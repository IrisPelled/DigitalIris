# Backend env. Copy to .env and set real values. Do not commit .env.
PORT=3001

# Required in production: strong random secret for session signing
SESSION_SECRET=change-me-in-production

# Admin password for staff login (list + export). No default in production.
ADMIN_PASSWORD=admin
